import { DashboardLayout } from '../../components/DashboardLayout';
// FIXED: Parent subpages split to Attendance.tsx, Notifications.tsx, Feedback.tsx with real data.
// /parent uses Dashboard.tsx (working). index.tsx no longer needed for routing.
